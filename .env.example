# Database path (defaults to data/presale.db)
DATABASE_URL=./data/presale.db

# Admin secret for protected routes
ADMIN_SECRET=change-this-in-production

# CoinGecko API key (optional - free tier works without key)
COINGECKO_API_KEY=

# Price cache TTL in seconds
PRICE_CACHE_TTL=60
